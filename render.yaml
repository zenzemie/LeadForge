services:
  # Backend API
  - type: web
    name: ai-outreach-api
    env: node
    plan: free
    region: oregon
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: GOOGLE_PLACES_API_KEY
        sync: false
      - key: API_AUTH_KEY
        generateValue: true

  # Frontend Dashboard
  - type: web
    name: ai-outreach-dashboard
    env: static
    plan: free
    region: oregon
    buildCommand: cd client && npm install && npm run build
    staticPublishDir: client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          name: ai-outreach-api
          type: web
          property: host
      - key: VITE_API_AUTH_KEY
        fromService:
          name: ai-outreach-api
          type: web
          property: envVar
          envVarKey: API_AUTH_KEY
